{
  "routes": [
    {
      "method": "GET",
      "path": "/readings",
      "handler": "readings.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/readings/count",
      "handler": "readings.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/readings/:id",
      "handler": "readings.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/readings",
      "handler": "readings.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/readings/:id",
      "handler": "readings.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/readings/:id",
      "handler": "readings.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
